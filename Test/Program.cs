using Durak_BL.Enums;
using Durak_BL.Model;
using Durak_BL.Controller;
using Durak_BL;

namespace Test
{
    internal class Program
    {
        static void Main(string[] args)
        {
            


            //List<PlayerController> players = new List<PlayerController>();
            //players.Add(new PlayerController(new Account("Bob", 5000)));
            //players.Add(new PlayerController(new Account("Nick", 5000)));

            //GameController game = new GameController(ref players);
            //game.Players[0].Player.Cards.Sort();
            //Console.WriteLine(game.Players[0].Player.ToString());
            //Console.WriteLine(game.Players[1].Player.ToString());

            //Console.WriteLine(game.PackController.Pack.ToString());
        }
    }
}